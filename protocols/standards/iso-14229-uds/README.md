# ISO 14229 UDS

UDS is an application layer commonly transported by ISO-TP.  It can expose
module-specific identification and data identifiers that are outside the
standard emissions PIDs.  This project treats UDS as an experimental,
read-only capability: no session changes, security access, coding, clearing,
or actuator control is permitted by a profile.
